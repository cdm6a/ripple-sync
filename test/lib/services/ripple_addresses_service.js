var rippleAddresses = require(__dirname + '/../../../lib/services/ripple_addresses_service.js');

describe('RippleAddressesService', function() {

  it('should fetch all ripple addresses', function(done) {

  });
});
